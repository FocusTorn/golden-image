<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Golden Imager" 
        MinWidth="1130" MinHeight="600"
        MaxWidth="1400"
        ResizeMode="CanResize" 
        SnapsToDevicePixels="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{DynamicResource FgColor}">
    <Window.Resources>
        <!-- Sort column header hover style -->
        <Style x:Key="SortHeaderBtnStyle" TargetType="StackPanel">
            <Setter Property="Opacity" Value="1.0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.75"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- WinDots1 (Windows-style dotted preloader) -->
        <Style x:Key="WinDots1Style" TargetType="Control">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Grid x:Name="Ring" Background="Transparent" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" RenderTransformOrigin="0.5,0.5">
                            <Grid.Resources>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource ButtonBg}"/>
                                    <Setter Property="Width" Value="6"/>
                                    <Setter Property="Height" Value="6"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Top"/>
                                    <Setter Property="RenderTransformOrigin" Value="0.5,4"/> <!-- Pivot around center of 48x48 -->
                                </Style>
                            </Grid.Resources>
                            <Ellipse x:Name="dot1"><Ellipse.RenderTransform><RotateTransform x:Name="rot1" Angle="0"/></Ellipse.RenderTransform></Ellipse>
                            <Ellipse x:Name="dot2"><Ellipse.RenderTransform><RotateTransform x:Name="rot2" Angle="0"/></Ellipse.RenderTransform></Ellipse>
                            <Ellipse x:Name="dot3"><Ellipse.RenderTransform><RotateTransform x:Name="rot3" Angle="0"/></Ellipse.RenderTransform></Ellipse>
                            <Ellipse x:Name="dot4"><Ellipse.RenderTransform><RotateTransform x:Name="rot4" Angle="0"/></Ellipse.RenderTransform></Ellipse>
                            <Ellipse x:Name="dot5"><Ellipse.RenderTransform><RotateTransform x:Name="rot5" Angle="0"/></Ellipse.RenderTransform></Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="rot1" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:2"><DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseInOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="rot2" Storyboard.TargetProperty="Angle" From="0" To="360" BeginTime="0:0:0.15" Duration="0:0:2"><DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseInOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="rot3" Storyboard.TargetProperty="Angle" From="0" To="360" BeginTime="0:0:0.3" Duration="0:0:2"><DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseInOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="rot4" Storyboard.TargetProperty="Angle" From="0" To="360" BeginTime="0:0:0.45" Duration="0:0:2"><DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseInOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="rot5" Storyboard.TargetProperty="Angle" From="0" To="360" BeginTime="0:0:0.6" Duration="0:0:2"><DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseInOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Feature Card Style (Used in Settings Dashboard) -->
        <Style x:Key="FeatureCardStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="{DynamicResource CardBgColor}"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBg}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBg}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource ButtonDisabled}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource ButtonDisabled}"/>
                    <Setter Property="Foreground" Value="{DynamicResource ButtonTextDisabled}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ButtonHover}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource ButtonHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ButtonPressed}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource ButtonPressed}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Modern Blue Button Style for Dashboard Actions -->
        <Style x:Key="ActiveBlueButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Width" Value="150"/> 
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <!-- ToggleSwitch style: pill track + sliding thumb (Windows 11 style) -->
        <Style x:Key="ToggleSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Track" Width="44" Height="24" CornerRadius="12" Background="{DynamicResource ComboBgColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                            <Ellipse x:Name="Thumb" Width="18" Height="18" Margin="3" HorizontalAlignment="Left" Fill="White">
                                <Ellipse.RenderTransform>
                                    <TranslateTransform x:Name="ThumbTransform" X="0"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource ButtonBg}"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="20" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,1"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource SecondaryButtonDisabled}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                    <Setter Property="Foreground" Value="{DynamicResource SecondaryButtonTextDisabled}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryButtonHover}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderColor}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryButtonPressed}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Sidebar Icon Style -->
        <Style x:Key="SidebarIconStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Foreground" Value="{DynamicResource LabelColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#25FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- OldWinBars1 (Classic rectangular spinning bars) -->
        <Style x:Key="OldWinBars1Style" TargetType="Control">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                            <Grid.Resources>
                                <Style TargetType="Rectangle">
                                    <Setter Property="Fill" Value="{DynamicResource ButtonBg}"/>
                                    <Setter Property="Width" Value="3"/>
                                    <Setter Property="Height" Value="10"/>
                                    <Setter Property="RadiusX" Value="1.5"/>
                                    <Setter Property="RadiusY" Value="1.5"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Top"/>
                                    <Setter Property="RenderTransformOrigin" Value="0.5,2.4"/> <!-- Pivot around center -->
                                </Style>
                            </Grid.Resources>
                            <Rectangle Opacity="1.00"><Rectangle.RenderTransform><RotateTransform Angle="0"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.91"><Rectangle.RenderTransform><RotateTransform Angle="30"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.82"><Rectangle.RenderTransform><RotateTransform Angle="60"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.73"><Rectangle.RenderTransform><RotateTransform Angle="90"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.64"><Rectangle.RenderTransform><RotateTransform Angle="120"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.55"><Rectangle.RenderTransform><RotateTransform Angle="150"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.46"><Rectangle.RenderTransform><RotateTransform Angle="180"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.37"><Rectangle.RenderTransform><RotateTransform Angle="210"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.28"><Rectangle.RenderTransform><RotateTransform Angle="240"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.19"><Rectangle.RenderTransform><RotateTransform Angle="270"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.10"><Rectangle.RenderTransform><RotateTransform Angle="300"/></Rectangle.RenderTransform></Rectangle>
                            <Rectangle Opacity="0.05"><Rectangle.RenderTransform><RotateTransform Angle="330"/></Rectangle.RenderTransform></Rectangle>
                            
                            <Grid.RenderTransform>
                                <RotateTransform x:Name="GlobalRot" Angle="0" CenterX="24" CenterY="24"/>
                            </Grid.RenderTransform>
                            <Grid.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetName="GlobalRot" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Grid.Triggers>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Up/Down RepeatButton Style -->
        <Style x:Key="UpDownButtonStyle" TargetType="RepeatButton">
            <Setter Property="Background" Value="{DynamicResource ButtonBg}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ButtonHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ComboBgColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
            <Setter Property="MinHeight" Value="33"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Left accent line -->
                            <Border x:Name="ClosedAccentLine" Width="3" Height="18" HorizontalAlignment="Left" VerticalAlignment="Stretch" Background="{DynamicResource ButtonBg}" CornerRadius="1.5" Panel.ZIndex="2"/>
                            <ToggleButton x:Name="ToggleButton" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Focusable="False" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                                        <TextBlock x:Name="Arrow"
                                                                Text="&#xE70D;"
                                                                FontFamily="Segoe Fluent Icons"
                                                                FontSize="10"
                                                                HorizontalAlignment="Right"
                                                                VerticalAlignment="Center"
                                                                Margin="0,0,8,0"
                                                                Foreground="{DynamicResource FgColor}"
                                                                RenderTransformOrigin="0.5,0.5">
                                                            <TextBlock.RenderTransform>
                                                                <RotateTransform x:Name="ArrowRotation" Angle="0"/>
                                                            </TextBlock.RenderTransform>
                                                        </TextBlock>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Trigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="Arrow" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" To="180" Duration="0:0:0.2">
                                                                            <DoubleAnimation.EasingFunction>
                                                                                <CubicEase EasingMode="EaseOut"/>
                                                                            </DoubleAnimation.EasingFunction>
                                                                        </DoubleAnimation>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.EnterActions>
                                                            <Trigger.ExitActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="Arrow" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" To="0" Duration="0:0:0.2">
                                                                            <DoubleAnimation.EasingFunction>
                                                                                <CubicEase EasingMode="EaseOut"/>
                                                                            </DoubleAnimation.EasingFunction>
                                                                        </DoubleAnimation>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.ExitActions>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentPresenter"
                                            IsHitTestVisible="False"
                                            Margin="10,0,20,0"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                            <Popup x:Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Focusable="False"
                                PopupAnimation="Fade"
                                StaysOpen="False"
                                PlacementTarget="{Binding ElementName=ToggleButton}"
                                VerticalOffset="1"
                                HorizontalOffset="0">
                                <Grid x:Name="DropDown" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource ComboItemBgColor}"
                                            BorderBrush="{DynamicResource BorderColor}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="5,4,5,1">
                                        <ScrollViewer Margin="0,2,0,0"
                                                    VerticalScrollBarVisibility="Auto"
                                                    HorizontalScrollBarVisibility="Disabled">
                                            <ItemsPresenter Margin="0,0,0,1"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="SelectedIndex" Value="0">
                                <Setter TargetName="ClosedAccentLine" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="SelectedIndex" Value="-1">
                                <Setter TargetName="ClosedAccentLine" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ClosedAccentLine" Property="Visibility" Value="Collapsed"/>
                                <Setter Property="Background" Value="{DynamicResource ButtonDisabled}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ButtonTextDisabled}"/>
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Grid>
                            <Border x:Name="ItemBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <!-- Left accent line -->
                            <Border x:Name="AccentLine"
                                    Width="3"
                                    Height="15"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Stretch"
                                    Background="{DynamicResource ButtonBg}"
                                    CornerRadius="1.5"
                                    Margin="0"
                                    Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers> 
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="AccentLine" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ComboItemSelectedColor}"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ComboItemHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBlock Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>

        <!-- Label text block style for sections -->
        <Style x:Key="LabelTextBlockStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource LabelColor}"/>
            <Setter Property="FontSize" Value="{DynamicResource LabelFontSize}"/>
            <Setter Property="FontFamily" Value="{DynamicResource LabelFontFamily}"/>
            <Setter Property="FontWeight" Value="{DynamicResource LabelFontWeight}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Label Border Style for ComboBox labels -->
        <Style x:Key="LabelBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Margin" Value="-4,-2,-4,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <!-- Category card border style -->
        <Style x:Key="CategoryCardBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="{DynamicResource CardBgColor}"/>
            <Setter Property="Padding" Value="20,16,20,20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Category header text style -->
        <Style x:Key="CategoryHeaderTextBlock" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource CardTitleFontFamily}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Foreground" Value="{DynamicResource CardTitleColor}"/>
        </Style>

        <!-- Category sub-header text style -->
        <Style x:Key="CategorySubHeaderTextBlock" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource CardTitleFontFamily}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="Foreground" Value="{DynamicResource CardTitleColor}"/>
        </Style>

        <!-- Category header icon style -->
        <Style x:Key="CategoryHeaderIcon" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource IconFontFamily}"/>
            <Setter Property="FontSize" Value="19"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Margin" Value="0,0,8,12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Category help link button/text styles -->
        <Style x:Key="CategoryHelpLinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="6,1,0,13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonBg}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource ButtonHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource ButtonPressed}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CategoryHelpLinkTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{DynamicResource HelpLinkFontSize}"/>
            <Setter Property="FontWeight" Value="{DynamicResource HelpLinkFontWeight}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground">
                <Setter.Value>
                    <Binding RelativeSource="{RelativeSource AncestorType=Button}" Path="Foreground"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Overview changes text style -->
        <Style x:Key="OverviewNoChangesTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource BodyColor}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}"/>
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>

        <Style x:Key="OverviewChangeBulletStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource BodyColor}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- TextBox Style for search -->
        <Style TargetType="TextBox">
            <Setter Property="CaretBrush" Value="{DynamicResource FgColor}"/>
        </Style>

        <!-- User TextBox Style with disabled state -->
        <Style x:Key="UserTextBoxStyle" TargetType="TextBox">
            <Setter Property="CaretBrush" Value="{DynamicResource FgColor}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="{DynamicResource SearchFontSize}"/>
            <Setter Property="Margin" Value="1,0,0,1"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource ButtonDisabled}"/>
                    <Setter Property="Foreground" Value="{DynamicResource ButtonTextDisabled}"/>
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- User TextBox Border Style with disabled state -->
        <Style x:Key="UserTextBoxBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ComboBgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,6"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource ButtonDisabled}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="CheckBoxBorder" Grid.Column="0" Width="18" Height="18" Background="{DynamicResource CheckBoxBgColor}" BorderBrush="{DynamicResource CheckBoxBorderColor}" BorderThickness="1" CornerRadius="4" Margin="0,0,8,0">
                                    <TextBlock x:Name="CheckMark" Text="&#xE73E;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource ButtonBg}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,2"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource CheckBoxHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource ButtonBg}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource ButtonBg}"/>
                                <Setter TargetName="CheckMark" Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource ButtonDisabled}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ButtonTextDisabled}"/>
                                <Setter Property="Opacity" Value="0.6"/>
                                <Setter TargetName="CheckMark" Property="Foreground" Value="{DynamicResource ButtonTextDisabled}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource ButtonHover}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource ButtonHover}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox style for feature toggles -->
        <Style x:Key="FeatureCheckboxStyle" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Margin" Value="-4,-2,-4,10"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>

        <!-- CheckBox style for Stages Audit R/P/L - no content, centered box only -->
        <Style x:Key="StagesCheckboxStyle" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="IsThreeState" Value="True"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="CheckBoxBorder" Width="18" Height="18" Background="{DynamicResource CheckBoxBgColor}" BorderBrush="{DynamicResource CheckBoxBorderColor}" BorderThickness="1" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock x:Name="CheckMark" Text="&#xE73E;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource ButtonBg}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource CheckBoxHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource ButtonBg}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource ButtonBg}"/>
                                <Setter TargetName="CheckMark" Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource ButtonDisabled}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                                <Setter TargetName="CheckMark" Property="Foreground" Value="{DynamicResource ButtonTextDisabled}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox style for system-applied tweaks: 3-state cycle (star=ignore, check=reinstall, empty=revert) -->
        <Style x:Key="FeatureCheckboxSystemAppliedStyle" TargetType="CheckBox" BasedOn="{StaticResource FeatureCheckboxStyle}">
            <Setter Property="IsThreeState" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="CheckBoxBorder" Grid.Column="0" Width="18" Height="18" Background="{DynamicResource CheckBoxBgColor}" BorderBrush="{DynamicResource CheckBoxBorderColor}" BorderThickness="1" CornerRadius="4" Margin="0,0,8,0">
                                    <TextBlock x:Name="CheckMark" Text="&#xE73E;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource FgColor}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,2"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource CheckBoxHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="CheckMark" Property="Text" Value="&#xE735;"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckMark" Property="Foreground" Value="{DynamicResource FgColor}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Text" Value="&#xE73E;"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource ButtonBg}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource ButtonBg}"/>
                                <Setter TargetName="CheckMark" Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource ButtonDisabled}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ButtonTextDisabled}"/>
                                <Setter Property="Opacity" Value="0.6"/>
                                <Setter TargetName="CheckMark" Property="Foreground" Value="{DynamicResource ButtonTextDisabled}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource ButtonHover}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource ButtonHover}"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="{x:Null}"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource CheckBoxHoverColor}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox style for apps panels -->
        <Style x:Key="AppsPanelCheckBoxStyle" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Margin" Value="2,3,2,3"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- TextBlock style for App ID column in apps table -->
        <Style x:Key="AppIdTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource AppIdColor}"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- TextBlock style for App Name column in apps table -->
        <Style x:Key="AppNameTextStyle" TargetType="TextBlock">
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- TextBlock style for Description column in apps table -->
        <Style x:Key="AppDescTextStyle" TargetType="TextBlock">
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,0,8,0"/>
        </Style>

        <!-- Column widths for the apps table row grid -->
        <GridLength x:Key="AppTableCol0Width">160</GridLength>
        <GridLength x:Key="AppTableCol1Width">1*</GridLength>
        <GridLength x:Key="AppTableCol2Width">286</GridLength>

        <!-- Title Bar Button Style -->
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="0,8,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource CloseHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TitlebarButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryButtonHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryButtonPressed}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- SearchBox Border Style -->
        <Style x:Key="SearchBoxBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ComboBgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="Padding" Value="8,6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ComboHoverColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ContextMenu Style -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource CardBgColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel IsItemsHost="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- MenuItem Style -->
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" ContentSource="Icon" 
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header" 
                                                VerticalAlignment="Center"
                                                Margin="8,0,0,1"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ComboItemHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ComboItemSelectedColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource BorderColor}" 
            BorderThickness="1" 
            CornerRadius="8" 
            Background="{DynamicResource BgColor}"
            Margin="25">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                            Opacity="0.15" 
                            BlurRadius="20" 
                            ShadowDepth="0" 
                            Direction="0"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Resize Borders -->
            <Rectangle x:Name="ResizeLeft" Width="5" HorizontalAlignment="Left" Fill="Transparent" Cursor="SizeWE" Grid.Row="0" Grid.RowSpan="3" Panel.ZIndex="100"/>
            <Rectangle x:Name="ResizeRight" Width="5" HorizontalAlignment="Right" Fill="Transparent" Cursor="SizeWE" Grid.Row="0" Grid.RowSpan="3" Panel.ZIndex="100"/>
            <Rectangle x:Name="ResizeTop" Height="5" VerticalAlignment="Top" Fill="Transparent" Cursor="SizeNS" Grid.Row="0" Panel.ZIndex="100"/>
            <Rectangle x:Name="ResizeBottom" Height="5" VerticalAlignment="Bottom" Fill="Transparent" Cursor="SizeNS" Grid.Row="2" Panel.ZIndex="100"/>
            <Rectangle x:Name="ResizeTopLeft" Width="8" Height="8" HorizontalAlignment="Left" VerticalAlignment="Top" Fill="Transparent" Cursor="SizeNWSE" Grid.Row="0" Panel.ZIndex="101"/>
            <Rectangle x:Name="ResizeTopRight" Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Top" Fill="Transparent" Cursor="SizeNESW" Grid.Row="0" Panel.ZIndex="101"/>
            <Rectangle x:Name="ResizeBottomLeft" Width="8" Height="8" HorizontalAlignment="Left" VerticalAlignment="Bottom" Fill="Transparent" Cursor="SizeNESW" Grid.Row="2" Panel.ZIndex="101"/>
            <Rectangle x:Name="ResizeBottomRight" Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom" Fill="Transparent" Cursor="SizeNWSE" Grid.Row="2" Panel.ZIndex="101"/>

            <!-- Custom Title Bar Anchor -->
            <Grid Grid.Row="0" x:Name="TitleBar">
                <Border Background="{DynamicResource MediumBgColor}" CornerRadius="8,8,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" 
                                   Text="Golden Imager" 
                                   Foreground="{DynamicResource FgColor}"
                                   VerticalAlignment="Center" 
                                   Margin="12,0,0,0"
                                   FontSize="12"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="MenuBtn" Content="&#xE700;" FontFamily="Segoe Fluent Icons" FontSize="15" Style="{StaticResource TitlebarButton}" ToolTip="Options" AutomationProperties.Name="Options">
                                <Button.ContextMenu>
                                    <ContextMenu x:Name="MainMenu">
                                        <MenuItem x:Name="MenuOptions" Header="Options" AutomationProperties.Name="Options">
                                            <MenuItem.Icon>
                                                <TextBlock Text="&#xE713;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="{DynamicResource FgColor}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem x:Name="MenuExportSettings" Header="Export settings" AutomationProperties.Name="Export settings">
                                            <MenuItem.Icon>
                                                <TextBlock Text="&#xE78C;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="{DynamicResource FgColor}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem x:Name="MenuImportSettings" Header="Import settings" AutomationProperties.Name="Import settings">
                                            <MenuItem.Icon>
                                                <TextBlock Text="&#xE8B5;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="{DynamicResource FgColor}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem x:Name="MenuDocumentation" Header="Documentation" AutomationProperties.Name="Documentation">
                                            <MenuItem.Icon>
                                                <TextBlock Text="&#xe736;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{DynamicResource FgColor}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem x:Name="MenuReportBug" Header="Report a bug" AutomationProperties.Name="Report a bug">
                                            <MenuItem.Icon>
                                                <TextBlock Text="&#xEBE8;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="{DynamicResource FgColor}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem x:Name="MenuLogs" Header="Logs" AutomationProperties.Name="Logs">
                                            <MenuItem.Icon>
                                                <TextBlock Text="&#xf000;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="{DynamicResource FgColor}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem x:Name="MenuAbout" Header="About" AutomationProperties.Name="About">
                                            <MenuItem.Icon>
                                                <TextBlock Text="&#xE946;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="{DynamicResource FgColor}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>
                            <Button x:Name="CloseBtn" Content="&#xE8BB;" Style="{StaticResource CloseButton}" ToolTip="Close" AutomationProperties.Name="Close"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- Main Content Grid: Sidebar + Headless Tabs Island -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/> <!-- Sidebar Column -->
                    <ColumnDefinition Width="*"/>  <!-- Main Content Column -->
                </Grid.ColumnDefinitions>

                <!-- Sidebar (Pinned Dark Pillar) -->
                <Border Grid.Column="0" Background="{DynamicResource BgColor}" BorderThickness="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <!-- Main Icons -->
                            <RowDefinition Height="*"/>    <!-- Spacer -->
                            <RowDefinition Height="Auto"/> <!-- Footer Icons -->
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,20">
                            <Button x:Name="SideHomeBtn" Style="{StaticResource SidebarIconStyle}" Tag="0" ToolTip="Home Dashboard">
                                <TextBlock Text="&#xE80F;" FontFamily="Segoe Fluent Icons" FontSize="22"/>
                            </Button>
                            <Button x:Name="SideAppsBtn" Style="{StaticResource SidebarIconStyle}" Tag="1" ToolTip="App Removal">
                                <TextBlock Text="&#xE71D;" FontFamily="Segoe Fluent Icons" FontSize="22"/>
                            </Button>
                            <Button x:Name="SideTweaksBtn" Style="{StaticResource SidebarIconStyle}" Tag="2" ToolTip="System Tweaks">
                                <TextBlock Text="&#xE90F;" FontFamily="Segoe Fluent Icons" FontSize="22"/>
                            </Button>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Margin="0,0,0,20">
                            <Button x:Name="SideSettingsBtn" Style="{StaticResource SidebarIconStyle}" Tag="Settings" ToolTip="Settings">
                                <TextBlock Text="&#xE713;" FontFamily="Segoe Fluent Icons" FontSize="22"/>
                            </Button>
                            <Button x:Name="SideExitBtn" Style="{StaticResource SidebarIconStyle}" Tag="Exit" ToolTip="Exit Application">
                                <TextBlock Text="&#xE7E8;" FontFamily="Segoe Fluent Icons" FontSize="22" Foreground="#FF5555"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Headless TabControl Floating Island -->
                <Border Grid.Column="1" Background="{DynamicResource MediumBgColor}" CornerRadius="8" Margin="10,5,10,5">
                    <TabControl Background="Transparent" BorderThickness="0" Margin="10" Padding="0" Name="MainTabControl">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" Background="{DynamicResource SecondaryButtonBg}" BorderThickness="0" CornerRadius="0" Padding="10,10" Margin="0">
                                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource ButtonBg}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter Property="Foreground" Value="{DynamicResource FgColor}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabControl.ItemContainerStyle>
                    <Style TargetType="TabItem">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </Style>
                </TabControl.ItemContainerStyle>

                <!-- Home Tab -->
                <TabItem Header="Home" x:Name="HomeTab">
                    <Grid Margin="20,10,20,20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Row 0, Column 0: Load Profiles -->
                        <Border Grid.Row="0" Grid.Column="0" CornerRadius="12" Padding="20,16,20,20" Margin="8" BorderThickness="1" BorderBrush="#363B40">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#2A2D32" Offset="0"/>
                                    <GradientStop Color="#1E2124" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="Load Profiles" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="65"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="ActiveProfileText" Text="(No file selected)" Style="{DynamicResource CardSubtitleStyle}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    <Button x:Name="HomeBrowseBtn" Content="Browse..." Style="{DynamicResource SecondaryButtonStyle}" Grid.Column="1" Height="30" Margin="4,0" AutomationProperties.Name="Browse profile"/>
                                    <Button x:Name="HomeLoadBtn" Content="Load" Style="{DynamicResource PrimaryButtonStyle}" Grid.Column="2" Height="30" AutomationProperties.Name="Load profile"/>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="65"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox x:Name="RecentProfilesCombo" Height="32" Margin="0" AutomationProperties.Name="Recent Profiles"/>
                                    <Button x:Name="HomeRecentLoadBtn" Content="Load" Style="{DynamicResource PrimaryButtonStyle}" Grid.Column="1" Height="32" Margin="4,0,0,0" AutomationProperties.Name="Load recent profile"/>
                                </Grid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="65"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox x:Name="DefaultProfilesCombo" Height="32" Margin="0" AutomationProperties.Name="Default Profiles"/>
                                    <Button x:Name="HomeDefaultLoadBtn" Content="Load" Style="{DynamicResource PrimaryButtonStyle}" Grid.Column="1" Height="32" Margin="4,0,0,0" AutomationProperties.Name="Load default profile"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Row 0, Column 2: System Modifications -->
                        <Border Grid.Row="0" Grid.Column="2" CornerRadius="12" Padding="20,16,20,20" Margin="8" BorderThickness="1" BorderBrush="#363B40">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#2A2D32" Offset="0"/>
                                    <GradientStop Color="#1E2124" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                            </Border.Effect>
                            <Grid MinHeight="120">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="System Modifications" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                <Control x:Name="HomeModSpinner" Style="{StaticResource OldWinBars1Style}" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="48" Height="48" Visibility="Visible" Panel.ZIndex="10"/>
                                <StackPanel x:Name="HomeModContent" Grid.Row="1" Visibility="Collapsed" Margin="0,8,0,0">
                                    <TextBlock Text="Features" Style="{StaticResource CategorySubHeaderTextBlock}"/>
                                    <CheckBox x:Name="HomeModUltimatePerf" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Ultimate Performance Plan" IsThreeState="True" AutomationProperties.Name="UltimatePerf"/>
                                    <CheckBox x:Name="HomeModHyperVNFS" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Install Hyper-V &amp; NFS Features" IsThreeState="True" AutomationProperties.Name="HyperVNFS"/>
                                    <Button x:Name="HomeApplyFeaturesBtn" Content="Apply Changes" Style="{DynamicResource PrimaryButtonStyle}" Height="28" Margin="0,8,0,0" HorizontalAlignment="Stretch" Padding="12,0,12,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Row 0, Column 4: Stages Audit -->
                        <Border Grid.Row="0" Grid.Column="4" CornerRadius="12" Padding="20,16,20,20" Margin="8" BorderThickness="1" BorderBrush="#363B40">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#2A2D32" Offset="0"/>
                                    <GradientStop Color="#1E2124" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                            </Border.Effect>
                            <Grid MinHeight="120">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Stages Audit" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                <TextBlock Text="R  P  L" FontSize="9" Grid.Row="0" HorizontalAlignment="Left" Margin="4,25,0,0" Foreground="{DynamicResource LabelColor}" FontFamily="Consolas"/>
                                <Control x:Name="HomeStagesAuditSpinner" Style="{StaticResource OldWinBars1Style}" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="48" Height="48" Visibility="Visible" Panel.ZIndex="10"/>
                                <ScrollViewer x:Name="HomeStagesAuditContent" Grid.Row="1" VerticalScrollBarVisibility="Auto" Visibility="Collapsed" Margin="0,8,0,0">
                                    <StackPanel x:Name="HomeStagesAuditPanel"/>
                                </ScrollViewer>
                            </Grid>
                        </Border>

                        <!-- Row 1, Column 0: Quick Fixes -->
                        <Border Grid.Row="1" Grid.Column="0" CornerRadius="12" Padding="20,16,20,20" Margin="8" BorderThickness="1" BorderBrush="#363B40">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#2A2D32" Offset="0"/>
                                    <GradientStop Color="#1E2124" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                            </Border.Effect>
                            <Grid MinHeight="80">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Quick Fixes" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                <UniformGrid Grid.Row="1" Columns="1" Margin="0,8,0,0">
                                    <Button x:Name="HomeFixClearTempBtn" Content="Clear Temporary Files" Style="{DynamicResource SecondaryButtonStyle}" Height="28" Margin="0,0,0,4" HorizontalAlignment="Stretch"/>
                                    <Button x:Name="HomeFixResetUpdatesBtn" Content="Reset Windows Update" Style="{DynamicResource SecondaryButtonStyle}" Height="28" Margin="0,0,0,4" HorizontalAlignment="Stretch"/>
                                    <Button x:Name="HomeFixCorruptionScanBtn" Content="System Corruption Scan" Style="{DynamicResource SecondaryButtonStyle}" Height="28" Margin="0,0,0,0" HorizontalAlignment="Stretch"/>
                                </UniformGrid>
                            </Grid>
                        </Border>

                        <!-- Row 1, Column 2: Connection Settings -->
                        <Border Grid.Row="1" Grid.Column="2" CornerRadius="12" Padding="20,16,20,20" Margin="8" BorderThickness="1" BorderBrush="#363B40">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#2A2D32" Offset="0"/>
                                    <GradientStop Color="#1E2124" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                            </Border.Effect>
                            <Grid MinHeight="120">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Connection Settings" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                <Control x:Name="HomeConnSpinner" Style="{StaticResource OldWinBars1Style}" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="48" Height="48" Visibility="Visible" Panel.ZIndex="10"/>
                                <StackPanel x:Name="HomeConnContent" Grid.Row="1" Visibility="Collapsed" Margin="0,8,0,0">
                                    <CheckBox x:Name="HomeConnLimitBlank" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Limit Blank Password Use" IsThreeState="True" AutomationProperties.Name="LimitBlank"/>
                                    <CheckBox x:Name="HomeConnWinRM" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Enable Remote Management (WinRM)" IsThreeState="True" AutomationProperties.Name="WinRM"/>
                                    <CheckBox x:Name="HomeConnKeyIso" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Enable CNG Key Isolation (KeyIso)" IsThreeState="True" AutomationProperties.Name="KeyIso"/>
                                    <CheckBox x:Name="HomeConnAdmin" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Enable Built-in Administrator" IsThreeState="True" AutomationProperties.Name="AdminAccount"/>
                                    <Button x:Name="HomeApplyConnBtn" Content="Apply Settings" Style="{DynamicResource PrimaryButtonStyle}" Height="28" Margin="0,8,0,0" HorizontalAlignment="Stretch" Padding="12,0,12,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Row 1, Column 4: Stage Install -->
                        <Border Grid.Row="1" Grid.Column="4" CornerRadius="12" Padding="20,16,20,20" Margin="8" BorderThickness="1" BorderBrush="#363B40">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#2A2D32" Offset="0"/>
                                    <GradientStop Color="#1E2124" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                            </Border.Effect>
                            <Grid MinHeight="120">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Stage Install" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                <Control x:Name="HomeExecSpinner" Style="{StaticResource OldWinBars1Style}" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="48" Height="48" Visibility="Visible" Panel.ZIndex="10"/>
                                <StackPanel x:Name="HomeExecContent" Grid.Row="1" Visibility="Collapsed" Margin="0,8,0,0">
                                    <CheckBox x:Name="HomeExec1" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Scoop (inc. Basic Tools &amp; Search)" AutomationProperties.Name="Stage1"/>
                                    <CheckBox x:Name="HomeExec2" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="MSVC Build Tools" AutomationProperties.Name="Stage2"/>
                                    
                                    <CheckBox x:Name="HomeExec3" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="System Apps" AutomationProperties.Name="Stage3"/>
                                    <StackPanel Margin="16,0,0,0">
                                        <CheckBox x:Name="HomeExec3_Chrome" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Chrome" AutomationProperties.Name="AppChrome"/>
                                        <CheckBox x:Name="HomeExec3_VSCode" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="VS Code" AutomationProperties.Name="AppVSCode"/>
                                        <CheckBox x:Name="HomeExec3_Git" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Git" AutomationProperties.Name="AppGit"/>
                                        <CheckBox x:Name="HomeExec3_Go" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Go" AutomationProperties.Name="AppGo"/>
                                        <CheckBox x:Name="HomeExec3_GitHubCLI" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="GitHub CLI" AutomationProperties.Name="AppGitHubCLI"/>
                                        <CheckBox x:Name="HomeExec3_UniGetUI" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="UniGetUI" AutomationProperties.Name="AppUniGetUI"/>
                                    </StackPanel>

                                    <CheckBox x:Name="HomeExec4" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Rust Finish &amp; Tooling" AutomationProperties.Name="Stage4"/>
                                    <CheckBox x:Name="HomeExec5" Style="{StaticResource FeatureCheckboxSystemAppliedStyle}" Content="Finalize &amp; Cleanup" AutomationProperties.Name="Stage5"/>
                                    <Button x:Name="HomeExecRunBtn" Content="Execute" Style="{DynamicResource PrimaryButtonStyle}" Height="32" Margin="0,12,0,0" HorizontalAlignment="Stretch"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        </Grid>
                </TabItem>

                <!-- App Removal Tab -->
                <TabItem Header="App Removal">
                    <DockPanel>
                        <Border DockPanel.Dock="Top" Padding="20,10,20,0">
                            <StackPanel>
                                <TextBlock Text="App Removal" FontWeight="{DynamicResource TabTitleFontWeight}" FontSize="{DynamicResource TabTitleFontSize}" FontFamily="{DynamicResource TabTitleFontFamily}" Margin="0,0,0,5" Foreground="{DynamicResource TabTitleColor}"/>
                                <TextBlock Text="Select which apps you want to remove from your system" FontSize="{DynamicResource CardSubtitleFontSize}" FontFamily="{DynamicResource CardSubtitleFontFamily}" Margin="0,0,0,20" Foreground="{DynamicResource CardSubtitleColor}" TextWrapping="Wrap"/>

                                <!-- Filter Options -->
                                <Grid Margin="0,0,0,12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
                                        <Button x:Name="DefaultAppsBtn" Content="Select Default Apps" ToolTip="Select the default selection of apps" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" Margin="0,0,10,0" AutomationProperties.Name="Select Default Apps"/>
                                        <Button x:Name="LoadLastUsedAppsBtn" Content="Select Last Used Selection" ToolTip="Select the apps that were selected the last time Golden Imager was run" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" Margin="0,0,10,0" AutomationProperties.Name="Select Last Used Selection"/>
                                        <Button x:Name="ClearAppSelectionBtn" Content="Clear Selection" ToolTip="Clear all selected apps" Style="{DynamicResource SecondaryButtonStyle}" Height="32"  Padding="10,0" Margin="0,0,10,0" AutomationProperties.Name="Clear Selection"/>
                                    </StackPanel>

                                    <Border x:Name="AppSearchBorder" Grid.Row="0" Grid.Column="3">
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource SearchBoxBorderStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=AppSearchBox, Path=IsFocused}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource InputFocusColor}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="{DynamicResource IconFontFamily}" FontSize="14" VerticalAlignment="Center" Margin="4,0,8,0" Foreground="{DynamicResource FgColor}"/>
                                            <TextBlock x:Name="AppSearchPlaceholder" Grid.Column="1" Text="Search app" Foreground="{DynamicResource FgColor}" Opacity="{DynamicResource SearchPlaceholderOpacity}" FontSize="{DynamicResource SearchFontSize}" Margin="3,0,0,1" VerticalAlignment="Center" IsHitTestVisible="False"/>
                                            <TextBox x:Name="AppSearchBox" Grid.Column="1" Background="Transparent" Foreground="{DynamicResource FgColor}" BorderThickness="0" FontSize="{DynamicResource SearchFontSize}" Margin="1,0,0,1" VerticalAlignment="Center" Text="" AutomationProperties.Name="Search app"/>
                                        </Grid>
                                    </Border>

                                    <!-- App Profile row with toggles right-justified -->
                                    <Grid Grid.Row="1" Grid.ColumnSpan="4" Margin="0,12,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                                            <TextBlock Text="App Profile:" FontFamily="{DynamicResource LabelFontFamily}" FontSize="{DynamicResource LabelFontSize}" FontWeight="{DynamicResource LabelFontWeight}" Foreground="{DynamicResource LabelColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <ComboBox x:Name="AppProfileCombo" MinWidth="180" Height="32" Margin="0,0,8,0" AutomationProperties.Name="App Profile"/>
                                            <Button x:Name="AppProfileReplaceBtn" Content="Load" ToolTip="Clear selection and load profile" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" Margin="0,0,8,0" AutomationProperties.Name="Load profile"/>
                                            <Button x:Name="AppProfileMergeBtn" Content="Merge" ToolTip="Add profile selections to current selection" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" Margin="0,0,8,0" AutomationProperties.Name="Merge profile"/>
                                            <Button x:Name="AppProfileSaveBtn" Content="Save" ToolTip="Update active profile or save as new" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" Margin="0,0,8,0" AutomationProperties.Name="Save profile"/>
                                            <Button x:Name="AppProfileSaveAsBtn" Content="Save as..." ToolTip="Save current selection to a new profile" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" Margin="0,0,8,0" AutomationProperties.Name="Save to new profile"/>
                                            <Button x:Name="AppProfileDeleteBtn" Content="Delete" ToolTip="Remove selected profile" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" AutomationProperties.Name="Delete profile"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                            <CheckBox x:Name="OnlyInstalledAppsBox" Content="Only show installed" IsChecked="False" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,16,0" ToolTip="Filter curated list to apps installed on this system (uses Get-AppxPackage)" AutomationProperties.Name="Only show installed"/>
                                            <CheckBox x:Name="ShowUserNotListedBox" Content="Show user not listed" IsChecked="False" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,16,0" ToolTip="User-installed apps (in profile, not provisioned) that are not in Apps.json" AutomationProperties.Name="Show user not listed"/>
                                            <CheckBox x:Name="ShowProvisionedNotListedBox" Content="Show provisioned not listed" IsChecked="False" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="0,0,16,0" ToolTip="Provisioned apps (master template) that are not in Apps.json" AutomationProperties.Name="Show provisioned not listed"/>
                                            <CheckBox x:Name="ShowAllNotListedBox" Content="Show all not listed" IsChecked="False" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" ToolTip="All installed/provisioned apps not in Apps.json" AutomationProperties.Name="Show all not listed"/>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </StackPanel>
                        </Border>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Apps List -->
                            <Grid Grid.Row="0" Margin="20,0,20,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <!-- Column Headers -->
                                <Border Grid.Row="0" Background="{DynamicResource TableHeaderColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1,1,1,0" CornerRadius="4,4,0,0">
                                    <Grid x:Name="AppHeaderGrid" Margin="26,6,8,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition x:Name="HeaderColDot" Width="16"/>
                                            <ColumnDefinition x:Name="HeaderColName" Width="151"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition x:Name="HeaderColDesc" Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition x:Name="HeaderColId" Width="261"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel x:Name="HeaderNameBtn" Grid.Column="1" Orientation="Horizontal" Cursor="Hand" VerticalAlignment="Center" Style="{StaticResource SortHeaderBtnStyle}">
                                            <TextBlock Text="Name" FontWeight="SemiBold" FontSize="16" Foreground="{DynamicResource FgColor}"/>
                                            <TextBlock x:Name="SortArrowName" Text="&#xE70E;" FontFamily="Segoe Fluent Icons" FontSize="11" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="5,1,0,0" Opacity="0.3" RenderTransformOrigin="0.5,0.5">
                                                <TextBlock.RenderTransform><RotateTransform Angle="0"/></TextBlock.RenderTransform>
                                            </TextBlock>
                                        </StackPanel>
                                        <GridSplitter Grid.Column="2" Width="9" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" Cursor="SizeWE">
                                            <GridSplitter.Template>
                                                <ControlTemplate TargetType="GridSplitter">
                                                    <Grid Background="Transparent">
                                                        <Rectangle Width="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" Fill="{DynamicResource BorderColor}" Opacity="0.35" Margin="0,2"/>
                                                    </Grid>
                                                </ControlTemplate>
                                            </GridSplitter.Template>
                                        </GridSplitter>
                                        <StackPanel x:Name="HeaderDescriptionBtn" Grid.Column="3" Orientation="Horizontal" Cursor="Hand" VerticalAlignment="Center" Margin="8,0,0,0" Style="{StaticResource SortHeaderBtnStyle}">
                                            <TextBlock Text="Description" FontWeight="SemiBold" FontSize="16" Foreground="{DynamicResource FgColor}"/>
                                            <TextBlock x:Name="SortArrowDescription" Text="&#xE70E;" FontFamily="Segoe Fluent Icons" FontSize="11" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="5,1,0,0" Opacity="0.3" RenderTransformOrigin="0.5,0.5">
                                                <TextBlock.RenderTransform><RotateTransform Angle="0"/></TextBlock.RenderTransform>
                                            </TextBlock>
                                        </StackPanel>
                                        <GridSplitter Grid.Column="4" Width="9" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" Cursor="SizeWE">
                                            <GridSplitter.Template>
                                                <ControlTemplate TargetType="GridSplitter">
                                                    <Grid Background="Transparent">
                                                        <Rectangle Width="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" Fill="{DynamicResource BorderColor}" Opacity="0.35" Margin="0,2"/>
                                                    </Grid>
                                                </ControlTemplate>
                                            </GridSplitter.Template>
                                        </GridSplitter>
                                        <StackPanel x:Name="HeaderAppIdBtn" Grid.Column="5" Orientation="Horizontal" Cursor="Hand" VerticalAlignment="Center" Style="{StaticResource SortHeaderBtnStyle}">
                                            <TextBlock Text="App ID" FontWeight="SemiBold" FontSize="16" Foreground="{DynamicResource FgColor}"/>
                                            <TextBlock x:Name="SortArrowAppId" Text="&#xE70E;" FontFamily="Segoe Fluent Icons" FontSize="11" Foreground="{DynamicResource FgColor}" VerticalAlignment="Center" Margin="5,1,0,0" Opacity="0.3" RenderTransformOrigin="0.5,0.5">
                                                <TextBlock.RenderTransform><RotateTransform Angle="0"/></TextBlock.RenderTransform>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <!-- Apps content -->
                                <Grid Grid.Row="1">
                                    <ScrollViewer VerticalScrollBarVisibility="Visible">
                                        <StackPanel x:Name="AppSelectionPanel" Margin="10,4,0,4"/>
                                    </ScrollViewer>
                                    <!-- Top fade gradient -->
                                    <Border IsHitTestVisible="False" VerticalAlignment="Top" Height="10" Margin="0,0,20,0" Background="{DynamicResource BgColor}">
                                        <Border.OpacityMask>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="Black" Offset="0"/>
                                                <GradientStop Color="Black" Offset="0.5"/>
                                                <GradientStop Color="Transparent" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.OpacityMask>
                                    </Border>
                                    <!-- Bottom fade gradient -->
                                    <Border IsHitTestVisible="False" VerticalAlignment="Bottom" Height="20" Margin="0,0,20,0" Background="{DynamicResource BgColor}">
                                        <Border.OpacityMask>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="Transparent" Offset="0"/>
                                                <GradientStop Color="Black" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.OpacityMask>
                                    </Border>
                                </Grid>
                            </Grid>
                            <Border Grid.Row="1" HorizontalAlignment="Stretch" Background="{DynamicResource BgColor}" Padding="0,15,0,0">
                                <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button x:Name="ToggleUnsafeAppsBtn" Grid.Column="0" Background="Transparent" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Left" Margin="30,0,0,0" AutomationProperties.Name="Toggle unsafe apps">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <TextBlock x:Name="LinkText" Text="Hide unsafe apps" FontSize="{DynamicResource ButtonSecondaryFontSize}" Foreground="{DynamicResource AccentColor}" FontWeight="{DynamicResource ButtonSecondaryFontWeight}" TextDecorations="Underline"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="LinkText" Property="Foreground" Value="{DynamicResource ButtonHover}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                    <Button x:Name="AppRemovalReviewBtn" Grid.Column="1" Background="Transparent" BorderThickness="0" Cursor="Hand" AutomationProperties.Name="Review selected changes">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <TextBlock x:Name="LinkText" Text="Review selected changes" FontSize="{DynamicResource ButtonSecondaryFontSize}" Foreground="{DynamicResource AccentColor}" FontWeight="{DynamicResource ButtonSecondaryFontWeight}" TextDecorations="Underline"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="LinkText" Property="Foreground" Value="{DynamicResource ButtonHover}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                    <TextBlock Grid.Column="2" x:Name="AppSelectionStatus" Text="0 apps selected" FontSize="{DynamicResource BodyFontSize}" Foreground="{DynamicResource BodyColor}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,30,0"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>
                    </DockPanel>
                </TabItem>

                <!-- Tweaks Tab -->
                <TabItem Header="Tweaks">
                    <DockPanel>
                        <Border DockPanel.Dock="Top" Padding="20,10,20,16" Background="{DynamicResource BgColor}">
                            <StackPanel>
                                <TextBlock Text="System Tweaks" FontWeight="{DynamicResource TabTitleFontWeight}" FontSize="{DynamicResource TabTitleFontSize}" FontFamily="{DynamicResource TabTitleFontFamily}" Margin="0,0,0,5" Foreground="{DynamicResource TabTitleColor}"/>
                                <TextBlock Text="Select which tweaks you want to apply to your system, hover over settings for more information" FontSize="{DynamicResource CardSubtitleFontSize}" FontFamily="{DynamicResource CardSubtitleFontFamily}" Margin="0,0,0,20" Foreground="{DynamicResource CardSubtitleColor}" TextWrapping="Wrap"/>
                            
                                <!-- Filter Options -->
                                <Grid Margin="0,0,0,12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock x:Name="TweakUpdateStatus" Grid.Row="0" Grid.ColumnSpan="3" Text="" Foreground="{DynamicResource BodyColor}" FontSize="{DynamicResource BodyFontSize}" Margin="0,0,0,6" Visibility="Collapsed"/>
                                    
                                    <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
                                        <Button x:Name="TweakUpdateBtn" Content="Update" ToolTip="Scan system and update selections based on applied tweaks" Style="{DynamicResource SecondaryButtonStyle}" Padding="10,0" Height="32" Margin="0,0,10,0" AutomationProperties.Name="Update from system"/>
                                        <Button x:Name="LoadDefaultsBtn" Content="Select Default Settings" ToolTip="Select the settings that are recommended for most people" Style="{DynamicResource SecondaryButtonStyle}" Padding="10,0" Height="32" Margin="0,0,10,0" AutomationProperties.Name="Select Default Settings"/>
                                        <Button x:Name="LoadLastUsedBtn" Content="Select Last Used Settings" ToolTip="Select the settings that were used the last time Golden Imager was run" Style="{DynamicResource SecondaryButtonStyle}" Padding="10,0" Height="32" Margin="0,0,10,0" AutomationProperties.Name="Select Last Used Settings"/>
                                        <Button x:Name="ClearAllTweaksBtn" Content="Clear Selection" ToolTip="Clear all selected tweaks" Style="{DynamicResource SecondaryButtonStyle}" Padding="10,0" Height="32" Margin="0,0,10,0" AutomationProperties.Name="Clear Selection"/>
                                    </StackPanel>

                                    <Border x:Name="TweakSearchBorder" Grid.Row="1" Grid.Column="2">
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource SearchBoxBorderStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=TweakSearchBox, Path=IsFocused}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource InputFocusColor}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="{DynamicResource IconFontFamily}" FontSize="14" VerticalAlignment="Center" Margin="4,0,8,0" Foreground="{DynamicResource FgColor}"/>
                                            <TextBlock x:Name="TweakSearchPlaceholder" Grid.Column="1" Text="Search setting" Foreground="{DynamicResource FgColor}" Opacity="{DynamicResource SearchPlaceholderOpacity}" FontSize="{DynamicResource SearchFontSize}" Margin="3,0,0,1" VerticalAlignment="Center" IsHitTestVisible="False"/>
                                            <TextBox x:Name="TweakSearchBox" Grid.Column="1" Background="Transparent" Foreground="{DynamicResource FgColor}" BorderThickness="0" FontSize="{DynamicResource SearchFontSize}" Margin="1,0,0,1" VerticalAlignment="Center" Text=""/>
                                        </Grid>
                                    </Border>
                                </Grid>

                                <!-- Tweak Profile row (GuiFork) -->
                                <StackPanel Orientation="Horizontal" Margin="0,12,0,8">
                                    <TextBlock Text="Tweak Profile:" FontFamily="{DynamicResource LabelFontFamily}" FontSize="{DynamicResource LabelFontSize}" FontWeight="{DynamicResource LabelFontWeight}" Foreground="{DynamicResource LabelColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <ComboBox x:Name="TweakProfileCombo" MinWidth="180" Height="32" Margin="0,0,8,0" AutomationProperties.Name="Tweak Profile"/>
                                    <Button x:Name="TweakProfileReplaceBtn" Content="Load" ToolTip="Clear selection and load profile" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" Margin="0,0,8,0" AutomationProperties.Name="Load profile"/>
                                    <Button x:Name="TweakProfileMergeBtn" Content="Merge" ToolTip="Add profile selections to current selection" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" Margin="0,0,8,0" AutomationProperties.Name="Merge profile"/>
                                    <Button x:Name="TweakProfileSaveBtn" Content="Save" ToolTip="Update active profile or save as new" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" Margin="0,0,8,0" AutomationProperties.Name="Save profile"/>
                                    <Button x:Name="TweakProfileSaveAsBtn" Content="Save as..." ToolTip="Save current selection to a new profile" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" Margin="0,0,8,0" AutomationProperties.Name="Save to new profile"/>
                                    <Button x:Name="TweakProfileDeleteBtn" Content="Delete" ToolTip="Remove selected profile" Style="{DynamicResource SecondaryButtonStyle}" Height="32" Padding="10,0" AutomationProperties.Name="Delete profile"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <ScrollViewer x:Name="TweaksScrollViewer" VerticalScrollBarVisibility="Auto" Margin="0,0,0,0" Padding="20,10,20,0">
                                    <Grid x:Name="TweaksGrid" Margin="0,10,0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Column 1: Dynamically populated -->
                                        <StackPanel Grid.Column="0" x:Name="Column0Panel" Margin="0,0,15,0"/>

                                        <!-- Column 2: Dynamically populated -->
                                        <StackPanel Grid.Column="1" x:Name="Column1Panel" Margin="0,0,15,0"/>

                                        <!-- Column 3: Dynamically populated -->
                                        <StackPanel Grid.Column="2" x:Name="Column2Panel"/>
                                    </Grid>
                                </ScrollViewer>
                                <!-- Top fade gradient -->
                                <Border IsHitTestVisible="False" VerticalAlignment="Top" Height="10" Margin="0,0,20,0" Background="{DynamicResource BgColor}">
                                    <Border.OpacityMask>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="Black" Offset="0"/>
                                            <GradientStop Color="Black" Offset="0.5"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.OpacityMask>
                                </Border>
                                <!-- Bottom fade gradient -->
                                <Border IsHitTestVisible="False" VerticalAlignment="Bottom" Height="20" Margin="0,0,20,0" Background="{DynamicResource BgColor}">
                                    <Border.OpacityMask>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="Transparent" Offset="0"/>
                                            <GradientStop Color="Black" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.OpacityMask>
                                </Border>
                                </Grid>

                                <!-- Review & Apply Section -->
                            <Border Grid.Row="1" HorizontalAlignment="Stretch" Background="{DynamicResource BgColor}" Padding="0,15,0,0">
                                <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button x:Name="TweaksReviewBtn" Grid.Column="1" Background="Transparent" BorderThickness="0" Cursor="Hand" AutomationProperties.Name="Review selected changes">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <TextBlock x:Name="LinkText" Text="Review selected changes" FontSize="{DynamicResource ButtonSecondaryFontSize}" Foreground="{DynamicResource AccentColor}" FontWeight="{DynamicResource ButtonSecondaryFontWeight}" TextDecorations="Underline"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="LinkText" Property="Foreground" Value="{DynamicResource ButtonHover}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                    <TextBlock Grid.Column="2" x:Name="TweakSelectionStatus" Text="0 selected | 0 enabled | 0 to revert" FontSize="{DynamicResource BodyFontSize}" Foreground="{DynamicResource BodyColor}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,30,0"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>
                    </DockPanel>
                </TabItem>

                <!-- Settings Dashboard Tab (NEW MASTER HUB) -->
                <TabItem x:Name="SettingsTab">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="20,10,20,20">
                                <TextBlock Text="Settings &amp; Support" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource FgColor}" Margin="0,0,0,20"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Column 0: Deployment -->
                                    <StackPanel Grid.Column="0">
                                        <Border Style="{StaticResource FeatureCardStyle}">
                                            <StackPanel>
                                                <TextBlock Text="Deployment" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                
                                                <!-- Apply Changes To subgroup -->
                                                <TextBlock Text="Apply changes to" Style="{StaticResource CategorySubHeaderTextBlock}"/>
                                                <ComboBox x:Name="UserSelectionCombo" AutomationProperties.Name="Target User">
                                                    <ComboBoxItem Content="All users and system" IsSelected="True"/>
                                                    <ComboBoxItem Content="Target user account"/>
                                                    <ComboBoxItem Content="Sysprep (audit mode)"/>
                                                </ComboBox>
                                                
                                                <Border x:Name="OtherUsernameBorder" Style="{StaticResource UserTextBoxBorderStyle}" Visibility="Collapsed" Margin="0,4,0,12">
                                                    <TextBox x:Name="OtherUsernameTextBox" Style="{StaticResource UserTextBoxStyle}" AutomationProperties.Name="Username"/>
                                                </Border>

                                                <!-- Remove Apps For subgroup -->
                                                <TextBlock Text="Remove apps for" Style="{StaticResource CategorySubHeaderTextBlock}" Margin="0,8,0,4"/>
                                                <ComboBox x:Name="AppRemovalScopeCombo" AutomationProperties.Name="App Removal Scope">
                                                    <ComboBoxItem x:Name="AppRemovalScopeAllUsers" Content="All users" IsSelected="True"/>
                                                    <ComboBoxItem x:Name="AppRemovalScopeCurrentUser" Content="Current user only"/>
                                                    <ComboBoxItem x:Name="AppRemovalScopeTargetUser" Content="Target user only" Visibility="Collapsed"/>
                                                </ComboBox>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>

                                    <!-- Column 2: Options -->
                                    <StackPanel Grid.Column="2">
                                        <Border Style="{StaticResource FeatureCardStyle}">
                                            <StackPanel>
                                                <TextBlock Text="Options" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                
                                                <!-- Toggles -->
                                                <Grid Margin="0,0,0,12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="Create Restore Point" VerticalAlignment="Center" Foreground="{DynamicResource FgColor}"/>
                                                    <ToggleButton x:Name="RestorePointToggle" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}" IsChecked="True"/>
                                                </Grid>

                                                <Grid Margin="0,0,0,12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="Auto-Restart Explorer" VerticalAlignment="Center" Foreground="{DynamicResource FgColor}"/>
                                                    <ToggleButton x:Name="RestartExplorerToggle" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}" IsChecked="True"/>
                                                </Grid>

                                                <Grid Margin="0,0,0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="Hide Launcher Window" VerticalAlignment="Center" Foreground="{DynamicResource FgColor}"/>
                                                    <ToggleButton x:Name="HideLauncherToggle" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}" IsChecked="False"/>
                                                </Grid>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>

                                    <!-- Column 4: Backup & Support -->
                                    <StackPanel Grid.Column="4">
                                        <!-- Backup Card -->
                                        <Border Style="{StaticResource FeatureCardStyle}">
                                            <StackPanel>
                                                <TextBlock Text="Backup &amp; Review" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                
                                                <Button x:Name="SettingsReviewAllBtn" Style="{StaticResource ActiveBlueButtonStyle}" HorizontalAlignment="Left" Width="150" Margin="0,0,0,10">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="&#xE73E;" FontFamily="{DynamicResource IconFontFamily}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="Review Settings" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Button>

                                                <Button x:Name="SettingsExportBtn" Content="Export Settings" Style="{StaticResource ActiveBlueButtonStyle}" HorizontalAlignment="Left" Width="150" Margin="0,0,0,10"/>
                                                <Button x:Name="SettingsImportBtn" Content="Import Settings" Style="{StaticResource ActiveBlueButtonStyle}" HorizontalAlignment="Left" Width="150"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- Support Card -->
                                        <Border Style="{StaticResource FeatureCardStyle}">
                                            <StackPanel>
                                                <TextBlock Text="Support &amp; Info" Style="{StaticResource CategoryHeaderTextBlock}"/>
                                                <Button x:Name="SettingsLogsBtn" Content="View Logs" Style="{StaticResource ActiveBlueButtonStyle}" HorizontalAlignment="Left" Width="150"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Sticky Bottom Apply Button -->
                        <Border Grid.Row="1" Background="{DynamicResource BgColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0" Padding="20,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Deployment Trigger" FontWeight="SemiBold" Foreground="{DynamicResource FgColor}"/>
                                    <TextBlock Text="Apply all selected tweaks, features and app removals to the system" FontSize="12" Foreground="{DynamicResource BodyColor}" Opacity="0.8"/>
                                </StackPanel>
                                <Button x:Name="DeploymentApplyBtn" Grid.Column="1" Content="Apply All Changes" Style="{StaticResource PrimaryButtonStyle}" Width="180" Height="40" FontSize="15" FontWeight="Bold"/>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>

        <!-- Status Bar Anchor -->
        <Border Grid.Row="2" Height="32" Background="{DynamicResource MediumBgColor}" CornerRadius="0,0,8,8">
                <Grid Margin="15,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="StatusBarIcon" Text="&#xE73E;" FontFamily="Segoe Fluent Icons" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource AccentColor}"/>
                        <TextBlock x:Name="StatusText" Text="System Ready" FontSize="11" VerticalAlignment="Center" Foreground="{DynamicResource LabelColor}"/>
                    </StackPanel>
                    <TextBlock x:Name="StatusBarVersion" Text="v1.0.0" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource LabelColor}" Opacity="0.5"/>
                </Grid>
            </Border>
            
            <!-- Modal Overlay for MessageBoxes -->
            <Rectangle x:Name="ModalOverlay" 
                       Grid.RowSpan="3" 
                       Fill="#80000000" 
                       Visibility="Collapsed" 
                       Panel.ZIndex="1000"
                       RadiusX="8"
                       RadiusY="8"
                       Margin="-1"/>
        </Grid>
    </Border>
</Window>
